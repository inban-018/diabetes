{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# Assignment - EDA & Data Visualization",
    "",
    "**Course:** 24ADI204 \u2013 Data Science and Visualization",
    "**Name:** _<Your Name>_",
    "**Roll Number:** _<Your Roll Number>_",
    "",
    "**Dataset:** Diabetes Dataset (scikit-learn built-in dataset, widely used in Kaggle-style diabetes progression analysis tutorials)",
    "",
    "This notebook contains 30 meaningful EDA plots covering Histograms, Boxplots, Violin plots, Scatter plots, Line plots, Bar plots, Heatmaps, Pie charts, Count plots and KDE plots."
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 1. Import Libraries & Load Dataset"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "import pandas as pd",
    "import numpy as np",
    "import matplotlib.pyplot as plt",
    "import seaborn as sns",
    "from sklearn.datasets import load_diabetes",
    "",
    "sns.set_theme(style=\"whitegrid\")",
    "",
    "diabetes = load_diabetes()",
    "df = pd.DataFrame(diabetes.data, columns=diabetes.feature_names)",
    "df['target'] = diabetes.target",
    "",
    "# Recover a readable Male/Female label from the standardized 'sex' feature",
    "df['sex_label'] = np.where(df['sex'] > 0, 'Male', 'Female')",
    "",
    "# Derived categorical column (Low / Medium / High) for boxplots, pie & count plots",
    "df['target_level'] = pd.cut(df['target'], bins=3, labels=['Low', 'Medium', 'High'])",
    "",
    "df.head()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 1: Histogram of Age",
    "Shows the distribution of the standardized age feature across all patients."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.histplot(df['age'], bins=20, kde=True, color='steelblue')",
    "plt.title('Distribution of Age')",
    "plt.xlabel('Age (standardized)')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 2: Histogram of BMI",
    "Displays the frequency distribution of BMI values."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.histplot(df['bmi'], bins=20, kde=True, color='darkorange')",
    "plt.title('Distribution of BMI')",
    "plt.xlabel('BMI (standardized)')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 3: Histogram of Blood Pressure",
    "Shows how average blood pressure is distributed across patients."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.histplot(df['bp'], bins=20, kde=True, color='seagreen')",
    "plt.title('Distribution of Blood Pressure')",
    "plt.xlabel('Blood Pressure (standardized)')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 4: Histogram of S5 (Serum Triglycerides Level)",
    "Displays the spread of the S5 blood serum measurement values."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.histplot(df['s5'], bins=20, kde=True, color='purple')",
    "plt.title('Distribution of S5 (Serum Triglycerides)')",
    "plt.xlabel('S5 (standardized)')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 5: Boxplot - BMI by Disease Progression Level",
    "Compares BMI across Low, Medium and High disease progression groups."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.boxplot(x='target_level', y='bmi', data=df, palette='Set2')",
    "plt.title('BMI by Disease Progression Level')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 6: Boxplot - Blood Pressure by Disease Progression Level",
    "Shows how blood pressure varies across disease progression groups."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.boxplot(x='target_level', y='bp', data=df, palette='Set3')",
    "plt.title('Blood Pressure by Disease Progression Level')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 7: Boxplot - S5 by Disease Progression Level",
    "Displays S5 spread and outliers for each disease progression group."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.boxplot(x='target_level', y='s5', data=df, palette='pastel')",
    "plt.title('S5 by Disease Progression Level')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 8: Boxplot - S6 by Disease Progression Level",
    "Compares the S6 blood serum measurement across disease progression groups."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.boxplot(x='target_level', y='s6', data=df, palette='cool')",
    "plt.title('S6 by Disease Progression Level')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 9: Violin Plot - BMI by Sex",
    "Combines boxplot and KDE to show the distribution shape of BMI for each sex."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.violinplot(x='sex_label', y='bmi', data=df, palette='muted')",
    "plt.title('BMI Distribution by Sex')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 10: Violin Plot - Blood Pressure by Sex",
    "Shows the distribution and density of blood pressure values within each sex."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.violinplot(x='sex_label', y='bp', data=df, palette='Set2')",
    "plt.title('Blood Pressure Distribution by Sex')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 11: Violin Plot - S3 by Disease Progression Level",
    "Displays the density shape of the S3 serum measurement per progression level."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.violinplot(x='target_level', y='s3', data=df, palette='coolwarm')",
    "plt.title('S3 Distribution by Disease Progression Level')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 12: Scatter Plot - BMI vs Disease Progression",
    "Explores the relationship between BMI and disease progression score."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.scatterplot(x='bmi', y='target', data=df, hue='sex_label')",
    "plt.title('BMI vs Disease Progression')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 13: Scatter Plot - Blood Pressure vs Disease Progression",
    "Shows correlation between blood pressure and disease progression, colored by sex."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.scatterplot(x='bp', y='target', data=df, hue='sex_label')",
    "plt.title('Blood Pressure vs Disease Progression')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 14: Scatter Plot - S5 vs Disease Progression",
    "Examines the relationship between S5 serum measurement and disease progression."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.scatterplot(x='s5', y='target', data=df, hue='sex_label')",
    "plt.title('S5 vs Disease Progression')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 15: Scatter Plot - Age vs BMI",
    "Explores how age and BMI relate across patients, colored by sex."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.scatterplot(x='age', y='bmi', data=df, hue='sex_label')",
    "plt.title('Age vs BMI')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 16: Line Plot - Sorted BMI Values",
    "Shows the trend of BMI values when sorted in ascending order across patients."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "plt.plot(sorted(df['bmi']), color='teal')",
    "plt.title('Sorted BMI Values Trend')",
    "plt.xlabel('Patient Index (sorted)')",
    "plt.ylabel('BMI')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 17: Line Plot - Mean Disease Progression by Level",
    "Shows how the mean disease progression score changes across the three levels."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "means = df.groupby('target_level')['target'].mean()",
    "plt.plot(means.index, means.values, marker='o', color='crimson')",
    "plt.title('Mean Disease Progression by Level')",
    "plt.xlabel('Level')",
    "plt.ylabel('Mean Target')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 18: Line Plot - Cumulative Sum of Sorted Blood Pressure",
    "Displays the cumulative sum of sorted blood pressure values, useful for spotting concentration."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "cum = np.cumsum(sorted(df['bp']))",
    "plt.plot(cum, color='darkgreen')",
    "plt.title('Cumulative Sum of Sorted Blood Pressure')",
    "plt.xlabel('Patient Index (sorted)')",
    "plt.ylabel('Cumulative Sum')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 19: Bar Plot - Mean BMI per Sex",
    "Compares average BMI between male and female patients."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.barplot(x='sex_label', y='bmi', data=df, estimator=np.mean, palette='Blues')",
    "plt.title('Mean BMI per Sex')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 20: Bar Plot - Mean Disease Progression per Sex",
    "Compares average disease progression score between male and female patients."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.barplot(x='sex_label', y='target', data=df, estimator=np.mean, palette='Oranges')",
    "plt.title('Mean Disease Progression per Sex')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 21: Bar Plot - Overall Feature Means (Selected Features)",
    "Shows the average value of a subset of clinical features across the whole dataset."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(8,6))",
    "sel_cols = ['age','bmi','bp','s1','s3','s5']",
    "feature_means = df[sel_cols].mean().sort_values()",
    "feature_means.plot(kind='barh', color='slateblue')",
    "plt.title('Overall Feature Means (Selected Features)')",
    "plt.xlabel('Mean Value')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 22: Bar Plot - Standard Deviation of Selected Features",
    "Displays the variability (std deviation) of a subset of clinical features."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(8,6))",
    "sel_cols = ['age','bmi','bp','s1','s3','s5']",
    "feature_std = df[sel_cols].std().sort_values()",
    "feature_std.plot(kind='barh', color='indianred')",
    "plt.title('Standard Deviation of Selected Features')",
    "plt.xlabel('Std Deviation')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 23: Heatmap - Correlation Matrix (All Features)",
    "Shows pairwise correlation between all numeric clinical features."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(10,8))",
    "num_cols = list(diabetes.feature_names) + ['target']",
    "corr = df[num_cols].corr()",
    "sns.heatmap(corr, cmap='coolwarm', annot=False)",
    "plt.title('Correlation Matrix - All Features')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 24: Heatmap - Correlation Matrix (Selected Features)",
    "Focused correlation heatmap on a subset of key clinical features."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,5))",
    "cols = ['age','bmi','bp','s1','s5','target']",
    "sns.heatmap(df[cols].corr(), cmap='viridis', annot=True, fmt='.2f')",
    "plt.title('Correlation Matrix - Selected Features')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 25: Heatmap - Mean Feature Values by Disease Progression Level",
    "Shows a pivot heatmap of mean feature values for each progression level."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(10,4))",
    "pivot = df.groupby('target_level')[['age','bmi','bp','s1','s5']].mean()",
    "sns.heatmap(pivot, cmap='YlGnBu', annot=True, fmt='.2f')",
    "plt.title('Mean Feature Values by Disease Progression Level')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 26: Pie Chart - Sex Distribution",
    "Shows the proportion of male vs female patients in the dataset."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,6))",
    "df['sex_label'].value_counts().plot(kind='pie', autopct='%1.1f%%', colors=sns.color_palette('Set2'))",
    "plt.title('Sex Distribution')",
    "plt.ylabel('')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 27: Pie Chart - Disease Progression Level Distribution",
    "Shows the proportion of samples falling into Low, Medium and High disease progression categories."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,6))",
    "df['target_level'].value_counts().plot(kind='pie', autopct='%1.1f%%', colors=sns.color_palette('pastel'))",
    "plt.title('Disease Progression Level Distribution')",
    "plt.ylabel('')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 28: Count Plot - Sex Counts",
    "Shows the number of male vs female patients as a count plot."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.countplot(x='sex_label', data=df, palette='Set1')",
    "plt.title('Count of Patients per Sex')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 29: Count Plot - Disease Progression Level Counts by Sex",
    "Shows the count of samples in each disease progression level, split by sex."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(7,4))",
    "sns.countplot(x='target_level', hue='sex_label', data=df, palette='Set2')",
    "plt.title('Disease Progression Level Counts by Sex')",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Plot 30: KDE Plot - BMI Distribution by Sex",
    "Shows smoothed density curves of BMI for male and female patients."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "plt.figure(figsize=(6,4))",
    "sns.kdeplot(data=df, x='bmi', hue='sex_label', fill=True, alpha=0.4)",
    "plt.title('BMI Density by Sex')",
    "plt.show()"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.10"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}